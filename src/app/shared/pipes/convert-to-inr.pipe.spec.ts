import { ConvertToInrPipe } from './convert-to-inr.pipe';

describe('ConvertToInrPipe', () => {
  it('create an instance', () => {
    const pipe = new ConvertToInrPipe();
    expect(pipe).toBeTruthy();
  });
});
